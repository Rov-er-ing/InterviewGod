{
  "name": "signal-detection-system",
  "version": "1.0.0",
  "description": "Signal Detection System for mass hiring events",
  "main": "api.py",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "serverless": "^4.0.0"
  }
}
